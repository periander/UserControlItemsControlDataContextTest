<UserControl x:Class="UserControlItemsControlDataContextTest.MyUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserControlItemsControlDataContextTest"
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="200"
             d:DataContext="{d:DesignInstance local:MyUserControlVM, IsDesignTimeCreatable=False}">
    
    <!-- Try commenting out MyUserControlVM.xaml's DataContext assignment and vice-versa, and see how it behaves in the designer and while executing. -->
    <!-- <UserControl.DataContext>
        <local:MyUserControlVM />
    </UserControl.DataContext> -->
    <!-- Try commenting out MyUserControlVM.xaml's DataContext assignment and vice-versa, and see how it behaves in the designer and while executing. -->


    <TextBlock>MyUserControl.AStr = '<Run Text="{Binding AStr}" />'</TextBlock>
</UserControl>
